# Base image for everything

pacstrap <<<<
    base
 
    -linux-firmware -dhcpcd -e2fsprogs -hwids -iptables -iputils -iproute2
    -jfsutils -linux -logrotate -lvm2 -man-db -man-pages -mdadm -nano
    -netctl -reiserfsprogs -s-nail -xfsprogs -usbutils -pciutils -inetutils
    -pcmciautils -vi -xfsprogs -texinfo

    dash systemd-sysvcompat pacman-mirrorlist
>>>>
